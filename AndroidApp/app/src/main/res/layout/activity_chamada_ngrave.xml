<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="14dp"
    tools:context=".activities.ChamadaActivity">

    <Button
        android:layout_above="@id/btn_informacoes"
        android:layout_marginBottom="15dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/AppTheme.CustomButton"
        android:text="GPS"
        android:background="@drawable/btn_ngrave"/>

    <Button
        android:layout_above="@id/fab_ngrave"
        android:layout_marginBottom="15dp"
        android:id="@+id/btn_informacoes"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:text="informações"
        android:textAllCaps="true"
        android:textSize="22sp"
        android:textStyle="bold"
        android:fontFamily="sans-serif"
        android:textColor="#FFFFFF"
        android:background="@drawable/btn_ngrave" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab_ngrave"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:fabSize="normal"
        android:layout_margin="2dp"
        android:src="@mipmap/ic_check_out"
        android:layout_alignParentBottom="true"
        android:onClick="checkout"
        android:layout_alignParentRight="true"/>

</RelativeLayout>